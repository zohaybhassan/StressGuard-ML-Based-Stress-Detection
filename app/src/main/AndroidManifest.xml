<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Supabase auth and data sync. Stress inference stays fully on-device and does not
         depend on this: the app must keep predicting with no network. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Sustained-stress alerts. The haptic is the alert; the notification is a fallback for
         when the app is backgrounded. POST_NOTIFICATIONS is runtime-granted from API 33, and
         the alert degrades to haptic-only without it rather than failing. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-permission android:name="android.permission.health.READ_SLEEP" />

    <queries>
        <package android:name="com.google.android.apps.healthdata" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StressGuard">
        <activity
            android:name=".ProfileSetupActivity"
            android:exported="false" />
        <activity
            android:name=".HealthChecklistActivity"
            android:exported="false" />
        <activity
            android:name=".SetPasswordActivity"
            android:exported="false" />
        <!-- singleTop so a tab tap returns to the existing instance rather than stacking a new
             one; BottomNav relies on this to keep the back stack from growing. -->
        <activity
            android:name=".TrendsActivity"
            android:exported="false"
            android:launchMode="singleTop" />
        <!-- adjustResize so the composer stays above the keyboard rather than under it. -->
        <activity
            android:name=".AssistantActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".HomeDashboardActivity"
            android:exported="false"
            android:launchMode="singleTop" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />

                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>

        <service
            android:name=".VitalReceiverService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />

                <data
                    android:host="*"
                    android:pathPrefix="/stress_vitals"
                    android:scheme="wear" />
            </intent-filter>
        </service>
    </application>

</manifest>
